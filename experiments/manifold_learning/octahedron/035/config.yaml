manifold:
  name: octahedron
  n_points: 20
  seed: 0
model:
  emb_dim: 24
  hidden_dim: 128
  num_heads: 4
  num_layers: 2
  dropout: 0.0
  normalize_embeddings: false
training:
  train_examples: 30000
  batch_size: 256
  steps: 50000
  learning_rate: 0.001
  weight_decay: 0.0001
  seed: 0
  progress_interval: 500
  test_fraction: 0.1
